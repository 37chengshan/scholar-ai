fastapi>=0.100.0,<0.110.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# PaperQA2 和 Docling
paper-qa>=5.0.0,<6.0.0
docling>=2.0.0,<3.0.0

# Zhipu AI SDK (替代 litellm)
zhipuai>=2.1.5

# 向量数据库
pgvector>=0.2.0
psycopg[binary]>=3.1.0
asyncpg>=0.29.0
# Embedding models
# sentence-transformers removed - using FlagEmbedding (BGE-M3) for 1024-dim embeddings
# Note: llama-index-embeddings-huggingface and paperqa still have sentence-transformers as transitive dependency
FlagEmbedding>=1.2.0  # BGE-M3 for 1024-dim embeddings and BGE-Reranker
adapters>=0.2.0  # For SPECTER 2

# Semantic chunking (per D-03)
llama-index-core>=0.10.0
llama-index-embeddings-huggingface>=0.1.0

# PyTorch
torch>=2.0.0
numpy>=1.24.0

# Milvus向量数据库
pymilvus>=2.3.0

# CLIP/SigLIP多模态嵌入
# Qwen3-VL multimodal embedding
qwen-vl-utils>=0.0.14  # Qwen3-VL utilities
transformers>=4.57.0   # Upgraded for Qwen3-VL support
torchvision>=0.15.0
pillow>=10.0.0

# 图数据库
neo4j>=5.14.0

# 全文搜索
tantivy>=0.20.0

# 中文NLP (Chinese text processing)
jieba>=0.42.0

# 工具库
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.25.0
aiofiles>=23.0.0

# 日志
structlog>=23.0.0

# 缓存
redis>=5.0.1,<6.0.0

# Celery任务队列 (Phase 11)
celery==5.3.6
psutil==5.9.8  # 内存监控

# 开发工具
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0
httpx>=0.25.0
black>=23.0.0
isort>=5.12.0
reportlab>=4.0.0  # PDF generation for test fixtures

# 对象存储
minio>=7.2.0

# JWT认证 (内部服务通信)
pyjwt[crypto]==2.8.0
cryptography==42.0.5

# 文本相似度计算 (去重)
rapidfuzz>=3.0.0
# Adding tenacity for retry logic
tenacity>=8.0.0
scikit-learn>=1.3.0
python-jose
